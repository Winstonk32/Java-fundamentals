//Write a function called findMax that takes three numbers as arguments and returns the largest of the three.


function FindMax(a,b,c){
    return(Math.max(a,b,c));
}

console.log(FindMax(23, 95, 10));
